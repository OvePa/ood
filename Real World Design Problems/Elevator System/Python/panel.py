class ElevatorPanel:
    def __init__(self, floor_buttons, open_buttons, close_buttons):
        self.__floor_buttons = floor_buttons
        self.__open_buttons = open_buttons
        self.__close_buttons = close_buttons


class HallPanel:
    def __init__(self, up, down):
        self.__up = up
        self.__down = down
